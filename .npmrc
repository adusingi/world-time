# Cloudflare Pages (and other CI) set NODE_ENV=production, which makes
# `npm ci` omit devDependencies — but the build needs them (tsc, vite,
# tailwind, @vitejs/plugin-react). Force dev deps to always install.
include=dev
